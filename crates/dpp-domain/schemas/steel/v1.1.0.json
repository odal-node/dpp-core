{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schema.odal-node.io/dpp/steel-v1.1.0.json",
  "$comment": "DRAFT — NOT IN FORCE. This sector's status is `provisional` in the sector catalog: no delegated act binds it yet, so this schema is our best reading of an instrument that has not been ratified and its field set may change without a migration path. Structural validation against it is not evidence of compliance, and a passport issued against it may not be representable once the act lands. The go sign is a single edit: set \"status\": \"in_force\" in crates/dpp-domain/sectors/<key>.json. CI then requires this marker to be removed.",
  "title": "Odal Node — Iron & Steel Sector Data (v1.1.0)",
  "description": "Iron and Steel sector fields for EU ESPR carbon intensity and recycled content reporting. v1.1.0 renames countryOfProduction to countryOfOrigin for cross-sector naming consistency.",
  "type": "object",
  "required": [
    "gtin",
    "co2ePerTonneSteel",
    "recycledScrapContentPct",
    "productCategory",
    "countryOfOrigin",
    "productionRoute"
  ],
  "properties": {
    "gtin": {
      "type": "string",
      "pattern": "^[0-9]{14}$",
      "description": "14-digit Global Trade Item Number."
    },
    "co2ePerTonneSteel": {
      "type": "number",
      "minimum": 0,
      "description": "Carbon intensity in tonne CO2e per tonne of steel produced."
    },
    "recycledScrapContentPct": {
      "type": "number",
      "minimum": 0,
      "maximum": 100,
      "description": "Recycled scrap content as a percentage of total input material."
    },
    "productCategory": {
      "type": "string",
      "enum": [
        "flat",
        "long",
        "tube",
        "specialty",
        "other"
      ],
      "description": "Steel product category."
    },
    "countryOfOrigin": {
      "type": "string",
      "pattern": "^[A-Z]{2}$",
      "description": "ISO 3166-1 alpha-2 country of origin (production)."
    },
    "productionRoute": {
      "type": "string",
      "enum": [
        "blast-furnace",
        "electric-arc",
        "direct-reduction"
      ],
      "description": "Primary production route."
    },
    "annualProductionTonnes": {
      "type": [
        "number",
        "null"
      ],
      "minimum": 0,
      "description": "Annual production volume in tonnes (optional)."
    }
  },
  "additionalProperties": false
}
