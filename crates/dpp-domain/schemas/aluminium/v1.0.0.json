{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schema.odal-node.io/dpp/aluminium-v1.0.0.json",
  "$comment": "DRAFT — NOT IN FORCE. No act binding this product group is in force: every act the instrument catalog records as reaching it is provisional or anticipated, so this schema is our best reading of an instrument that has not been ratified and its field set may change without a migration path. Structural validation against it is not evidence of compliance, and a passport issued against it may not be representable once the act lands. The go sign is a single edit: set that binding's \"status\" to \"in_force\" in crates/dpp-domain/instruments/<id>.json. CI then requires this marker to be removed.",
  "title": "Odal Node — Aluminium Product Group Data (v1.0.0)",
  "description": "Aluminium sector fields for EU ESPR carbon intensity reporting, aligned with CBAM benchmarks. Applicability dates and regulatory status are held in the product group manifest, not here.",
  "type": "object",
  "required": [
    "gtin",
    "alloyGrade",
    "productionRoute",
    "co2ePerTonneKg",
    "recycledContentPct",
    "countryOfProduction"
  ],
  "properties": {
    "gtin": {
      "type": "string",
      "pattern": "^[0-9]{14}$",
      "description": "14-digit Global Trade Item Number.",
      "x-disclosure": "public"
    },
    "alloyGrade": {
      "type": "string",
      "minLength": 1,
      "description": "Alloy series designation, e.g. 1xxx, 3xxx, 5xxx, 6xxx.",
      "x-disclosure": "public"
    },
    "productionRoute": {
      "type": "string",
      "enum": [
        "primary",
        "secondary-recycled",
        "mixed"
      ],
      "description": "Production route: primary (Hall-Heroult electrolysis), secondary-recycled (scrap remelting), or mixed.",
      "x-disclosure": "public"
    },
    "co2ePerTonneKg": {
      "type": "number",
      "minimum": 0,
      "description": "Carbon intensity in kg CO2e per tonne of aluminium produced.",
      "x-disclosure": "public"
    },
    "recycledContentPct": {
      "type": "number",
      "minimum": 0,
      "maximum": 100,
      "description": "Recycled scrap content as a percentage of total input.",
      "x-disclosure": "public"
    },
    "countryOfProduction": {
      "type": "string",
      "pattern": "^[A-Z]{2}$",
      "description": "ISO 3166-1 alpha-2 country of primary production.",
      "x-disclosure": "public"
    },
    "annualProductionTonnes": {
      "type": [
        "number",
        "null"
      ],
      "minimum": 0,
      "description": "Annual production volume in tonnes (optional).",
      "x-disclosure": "public"
    }
  },
  "additionalProperties": false
}
