{
  "key": "schema-org",
  "title": "Schema.org",
  "authority": "Schema.org (W3C Community Group; Google, Microsoft, Yahoo, Yandex)",
  "layer": "foundational",
  "namespaceIri": "https://schema.org/",
  "usedFor": "Universal web fallback — generic metadata, dates, ratings, observations. Candidate `schema:` prefix for this project's JSON-LD context.",
  "licence": "CC BY-SA 3.0 — confirmed 2026-08-11 from `https://schema.org/docs/terms.html`: \"The Sponsors' copyrights in the schema are licensed to website publishers and other third parties under the Creative Commons Attribution-ShareAlike License (version 3.0).\" Citing a term IRI (what a `schema:` prefix mapping does) is normal, unrestricted practice; the ShareAlike clause would only matter if the schema itself were adapted and republished, which this project does not do.",
  "status": "tracked",
  "checkedOn": "2026-08-11",
  "source": "https://schema.org/dateCreated and https://schema.org/dateModified — fetched and read directly, cross-checked against `Passport.updated_at`'s actual write paths in `crates/dpp-domain/src/domain/passport/passport.rs`.",
  "finding": "`dateCreated`: \"The date on which the CreativeWork was created or the item was added to a DataFeed.\" `dateModified`: \"The date on which the CreativeWork was most recently modified or when the item's entry was modified within a DataFeed.\" Both `domainIncludes` CreativeWork and DataFeedItem, `rangeIncludes` Date/DateTime. **A real, evidenced near-miss, not a hypothetical one**: the only production write path for `Passport.updated_at` in this crate is `Passport::transition_to()`, which bumps it on a status transition (Draft→Published etc.) — there is no general-purpose field-edit method that touches it. So `updated_at` is, per this crate's own contract, a **record/state-machine timestamp**, not a content-edit timestamp — a `Draft` passport whose `materials` changed without a status transition would not bump it. Schema.org's `dateModified` is anchored to CreativeWork semantics (content freshness, the sense search engines read), which is a plausible but not confirmed match; whether engine-layer code (outside this repo) bumps `updated_at` on ordinary field edits is unverified and outside what `dpp-core` alone can answer.",
  "nextStep": "Stay on `dpp:createdAt`/`dpp:updatedAt` rather than restoring the `schema:` mapping — the one thing that could be verified here (the write-path contract) points at a real semantic gap, and this project's own standard is to prefer a claim that survives contact with our own code over one that only looks plausible. Revisit only if the engine layer's write behaviour is confirmed to match schema.org's broader 'content modified' sense, not just the status-transition sense dpp-core itself implements."
}
